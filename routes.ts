/**
 * An array of routed need authentication to use
 */
export const protectedRoute = ["/admin(.*)", "/farmer(.*)"];
/**
 * Admin route prefix
 */
export const adminRoutePrefix = "/admin";

/**
 * Farmer route prefix
 */
export const farmerRoutePrefix = "/farmer";
